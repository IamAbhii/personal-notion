{
  "name": "@personal-space/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "migrate:local": "wrangler d1 migrations apply personal-space --local",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "fractional-indexing": "^4.0.0",
    "hono": "^4.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.0",
    "@cloudflare/workers-types": "^5.20260810.1",
    "@vitest/coverage-istanbul": "^4.1.11",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
